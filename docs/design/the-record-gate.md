# The record gate — pointer

The design lives beside the engine whose record it reads:

**`forge-play/Forge` → `docs/design/the-record-gate.md`**

Seven mechanical gates over a workshop's exported Nestor bundle (argued,
negatives recorded, warrants hold, human verified, connected, deposited,
bundle at head), added beside this store's nine in `stores/promote_check.py`
and replacing none of them. When the gate functions are built they go in
this repo, beside `promote_check.py`, reading a bundle path from the
attestation; what they read is the engine's. Nothing here beyond this
pointer, on purpose: the store is where pieces are taken from and where the
promotion record lives, not where the design is written (operator,
2026-09-03).
