# stores/sap_gate.py needs real asymmetric signing (stdlib has none).
# Every other stores/ and tools/ script is stdlib-only by convention;
# this is the first exception, and it's a deliberate one — not something
# to work around with a hand-rolled scheme in a trust-boundary module.
cryptography>=41.0

# --- Forge engine extraction (2026-08-11 -> archived here 2026-08-18) -----
#
# checkpoint_memory.py (needed Nestor) and checkpoint_schedule.py (needed
# fsrs) used to live in stores/ and are the reason the two commented-out
# blocks below existed. Both modules were extracted to rudi193-cmd/Forge on
# 2026-08-11 and the store-side copies were archived to
# stores/_forge_extracted/ on 2026-08-18 (see that directory's README) — so
# nothing live in this store imports nestor or fsrs any more. The comments
# are kept, unmodified, as the historical record of that decision; the real
# pins for Nestor/fsrs now belong in rudi193-cmd/Forge's own dependency
# file, not here.
#
# stores/readiness_corpus.py's `assess` CLI now imports `forge.measure_panel`
# (the archived stores/measure_panel.py's replacement) to run the panel
# against a build. Soft/optional the same way fsrs is below: `bearings` and
# `assess-gates` need no import of the panel at all, so only `assess` is
# affected, and it fails closed with an actionable message when forge isn't
# installed (see readiness_corpus.py's `_cmd_assess`). Not on PyPI, so a git
# dependency, the same pattern the Nestor line below used. Left commented so
# the stdlib-only default `pip install -r` stays clean:
#   forge @ git+https://github.com/rudi193-cmd/Forge@master
#
# stores/checkpoint_memory.py (D9/D12, docs/design/the-forge.md) needs
# Nestor. Nestor is NOT on PyPI — `pip index versions nestor` returns "No
# matching distribution found" — so this is a git dependency, the same
# pattern apps/semantic-translator/pyproject.toml already uses for the same
# package in this repo. Left commented out on purpose: the SHA is not
# pinned yet (this is playground-tier code — stores/checkpoint_memory.py
# has not gone through stores/promote_check.py — and the design doc's own
# "Pin Nestor at promotion time the way terpsi's FLEET-READS.md pins Nestor
# SHA" line says the real pin happens at D13 promotion, not before), and an
# uncommented line with a literal `<PIN-THIS-AT-PROMOTION>` placeholder
# instead of a real ref would break `pip install -r` for everyone who runs
# it today — exactly the class of bug this comment exists to avoid, not
# reproduce. Whoever promotes this: uncomment the line below with a real
# commit SHA in place of the placeholder.
#   nestor @ git+https://github.com/rudi193-cmd/Nestor@<PIN-THIS-AT-PROMOTION> ; python_version >= "3.10"
#
# For local dev/testing against stores/checkpoint_memory.py right now,
# install Nestor editable from the sibling checkout instead:
#   pip install -e /workspace/nestor
# (verify that path in your own environment first — `ls /workspace/nestor`
# — it is not guaranteed to be there or at that path).
#
# A SEPARATE, unrelated consumer: `.mcp.json`'s "nestor" entry and
# tools/decisions_boot.py's best-effort cross-check (stores/decisions/
# README.md, "Consulting Nestor") need the `nestor` CONSOLE SCRIPT on PATH,
# not a Python import — they shell out to `nestor serve` as a subprocess and
# speak MCP/JSON-RPC to it, so they need no line in THIS file. That wiring is
# pinned independently, already at a real tag rather than waiting for
# promotion: `pip install "nestor @ git+https://github.com/rudi193-cmd/Nestor@v0.2.0"`.

# stores/checkpoint_schedule.py (bite 2's FSRS fold-in, D9 / docs/design/
# the-forge-fsrs.md) uses py-fsrs for real spaced-repetition review
# scheduling. UNLIKE Nestor above, this is a SOFT/OPTIONAL dependency
# (D-FSRS-4, settled soft): the module imports `fsrs` lazily and, when it is
# absent, degrades to a fixed-interval fallback — the resurface flow never
# requires it. It IS on PyPI, MIT-licensed, and dependency-light (only
# typing-extensions). Left commented so the stdlib-only default `pip install
# -r` stays clean; install it to get real FSRS scheduling instead of the
# fallback. Pin caps the major (FSRS majors change the algorithm/params, the
# same reason the engine caps at <1.0):
#   fsrs>=6.3,<7
