{
  "app_id": "the-forge",
  "majors": [
    "python"
  ],
  "relation": null,
  "anchor": null,
  "location": "apps/the-forge",
  "maker": "USER",
  "lane": null,
  "state": "building",
  "notes": "READINESS SEAM extended + audit-hardened 2026-08-16 (docs/design/the-forge-readiness.md D-R7): GATE BEARINGS added -- promote_check's gates now map to corpus controls, not just the panel's four instruments. One honest mapping (witnessed, verified_by!=author -> USEQ-E075330B 'prevent authors from self-approving material controls' -- the gate's floor IS that sentence); nine gates investigated and SKIPPED for cause (tests_green's only close control already claimed by execution; vault_leak is SAFE-specific; own_repo/host_repointed touch a 5-clause traceability control neither gate alone establishes; import_pure_core/inversion/semantic_seam architectural). assess_gates() INVERTS assess()'s asymmetry: a FAILED gate is direct first-party evidence a control is unmet (Fail, cited), a PASSED gate only clears a mechanical check (Blocked, never Pass -- witnessed's string check is not a human's ratification). Duck-typed on promote_check's (name,ok,detail) so the module still never imports promote_check; assess-gates CLI over a JSON gate-results file. DRIFT GUARD added (tools/readiness_drift.py, stdlib-only): reads every control_id across BEARINGS+GATE_BEARINGS and checks each still resolves in the injected corpus; exit 0/1/2 (clean/drift/corpus-unavailable), closing the 'upstream drift is undetected, nothing runs it' gap -- a ready-to-paste CI job is in the design doc, wiring it left as the human call (the corpus is a separate repo not checked out in this repo's CI). AUDIT-DRIVEN HARDENING (a Sonnet auditor reviewed the committed seam adversarially while two build-out agents extended it): (1) the no-Pass invariant was only a call-site guard (_refuse_to_mint_pass inside assess) -- an audit built a Verdict OUTSIDE assess and watched a Pass flow through note() which printed 'NO control is Pass' over a live one; moved the refusal onto the TYPE (Verdict.__post_init__ rejects Status.PASS) so no path present or future can construct a Pass verdict, and removed the now-dead _refuse_to_mint_pass (keeping it would be exactly the dead code this seam's own gate bearings flag). (2) _contained was check-then-use (resolved once to check, read_text re-resolved) -- a symlink swap between the two could be checked inside and read outside; now _resolved_within returns the resolved path and the read is taken from it (not atomic against a genuine race, but that needs write access inside the root, which already lets an attacker plant a control -- documented honestly, not overclaimed). (3) note() now counts Blocked explicitly instead of borne-failed, so a future Not Applicable can't be silently folded into Blocked. Audit confirmed sound and unbroken: symlink escapes blocked, _as_data holds vs RTL-override/zero-width/Zalgo/homoglyph/tag-char injection, counts correct, corpus parse exact (1421+8621=10042, zero silent drops), duplicate detection fires. Full store suite 511 passed, 5 skipped. Store-side (D1); apps/the-forge and the corpus repo untouched. READINESS SEAM landed 2026-08-16 (docs/design/the-forge-readiness.md): stores/readiness_corpus.py measures the measuring panel's coverage against an EXTERNAL control corpus instead of the five classes the panel wrote for itself -- the sigmap trap one level up (a harness grading itself against a ruler it cut). The corpus is rudi193-cmd/production-readiness-checklist, a fork of MarinJursic/production-readiness-checklist (MIT, v2.1.0, released 2026-08-15): 10,042 technology-neutral controls with stable IDs (grounded: parsed 1,421 PRC-* + 8,621 USEQ-*, matching its own scripts/validate.py constants). INJECTED, NEVER VENDORED -- the Almanac seam one axis over (stores/almanac/README.md: 'ship the mold and the reader; the wood stays with whoever grew it'), applied to a public STANDARD rather than a public list: corpus root comes from an explicit path or FORGE_READINESS_CORPUS, no default, absence raises CorpusUnavailable as a declared gap. The two control regexes are lifted from the corpus's OWN validator (upstream owns its format; a format change is CorpusUnavailable, not a looser pattern). UNTRUSTED INPUT read as data: nothing imported or executed, every path checked to resolve inside the corpus root (a symlink planted in docs/checklists/ cannot smuggle a control in -- the crown-jewel containment check applied to an injected corpus), every control string NFKC-normalized with control chars dropped and capped at 300 chars so it cannot forge a line in a queue item. ONE-DIRECTIONAL: assess() can Fail a control, never Pass one -- a finding fails with artifact+metric+the control's own file:line cited; a clean instrument leaves it Blocked (absence of a finding is not evidence, the corpus's rule 6 and the Forge's thesis in one sentence); Status.PASS exists only because a HUMAN records one with evidence/owner/release/date, and _refuse_to_mint_pass() RAISES rather than returning, because a convention is what a later edit forgets. Bearings are hand-authored (4 instruments, 6 controls, each verified present in the real corpus by a live test) -- keyword-matching against 10,042 control texts would manufacture dozens of unearned mappings, exactly the inference rule 6 forbids. THE FIRST REAL RUN CORRECTED THE DESIGN: the census flagged the-binder/web/binder.png at 95% of the build and the seam called PRC-07-015 ('Large binaries, generated files, and vendored code are controlled') Fail -- but a 95% PNG is evidence a large binary EXISTS, not that large binaries are UNCONTROLLED, and rule 5's bar for Fail is direct evidence the control is NOT MET. So Bearing gained on_finding: census declares BLOCKED (names the artifact, says it raises the control without answering it) while hygiene finding a committed error_log genuinely fails it. Seven playground builds had been called Fail by the size census alone; all seven are now Blocked-with-the-artifact-named. A bearing may not claim a finding supports a Pass -- the constructor raises. calibration is deliberately absent from the table (longitudinal, a claim ACROSS builds, so it cannot bear on a control scoped to one release). NO PERCENTAGE is reported (the corpus's rule 13: one blocker outweighs hundreds of passing controls) -- raw counts and the reason the ratio is missing; two instruments disagreeing on one control resolve to a single status with Fail winning, the per-instrument verdicts kept as the evidence trail. What it reports on apps/the-forge (grounded): 'controls with evidence from this panel: 1 of 10,042' where the panel's own note reads 2-of-5 classes on the same run -- both true, the second hard to misread as an all-clear. Store-side (D1); apps/the-forge untouched; the corpus repo untouched. 30 new tests; full store suite 495 passed, 5 skipped. ENROLLED as a library 2026-08-11 (docs/design/the-forge-promotion.md): the model side (the refuse-a-confident-wrong-answer engine) was extracted out of the stores/ monorepo into its own standing repo rudi193-cmd/forge, branch claude/continuing-projects-o0jm9a (NOT the default branch \u2014 merging it is the ratifier's act, Section 0.2). Both forks settled by USER: model side as a library; real package imports (the 13 spec_from_file_location sibling-loads retired). STRUCTURED TO THE HOMESTEAD CONVENTION (USER: 'look at the root homestead folder'): the base repo holds ONLY the engine, FLAT in forge/ (as homestead holds homestead.keep), and all runtime state hangs off one shared home ~/.forge (override FORGE_HOME), resolved in one place forge/paths.py the way homestead-law and homestead-ledger share ~/.homestead (Path.home() spelling per homestead I-19/I-20, the one the vault-leak linter can see). 18 engine modules flat (checkpoint loop + measuring panel + model_route/model_egress routing) + paths.py + vendored _ids.py (principal's builder-id charset only, not the 900-line identity core); tests/ = 12 files; README rewritten in the house style; pyproject.toml (library-clean) + promotion.json. model_egress's socket/urlparse moved to lazy (function-local) so the WHOLE flat package is network-free at import (pure_core = forge). The extraction is a re-runnable, auditable tool (tools/extract_forge_pkg.py), not a mystery diff \u2014 it reproduces the pushed forge/ tree byte-identically. promote_check against the pushed tree: PASS on own_repo, manifest, tests_green (154 passed, 1 skipped in place via pyproject pythonpath), vault_leak, import_pure_core (forge network-free at import), inversion (host not imported), semantic_seam (forge.checkpoint_memory:CheckpointMemory \u2014 loose recognition over the maker's sealed-decision corpus, injected via Nestor). witnessed now PASSES: verified_by=rudi193 (the human operator ratifies, distinct from author vishwakarma, Section 0.2 \u2014 a human checking a machine's work, recorded on the forge feature branch 2026-08-11). The ONLY remaining gate is host_repointed, HELD by USER pending the engine landing on forge's default branch (the ratifying push to main was auto-blocked for the author \u2014 the operator's act or an explicit grant), after which the host is repointed (delete the stores/ model-side modules + tests, declare the-forge a dependency; rule 8) and promote_check --record writes stores/python/promoted/the-forge.json. Remaining: repoint the host + flip host_repointed; a second seat sets verified_by and runs promote_check --record to write stores/python/promoted/the-forge.json. Two soft-Nestor 'degraded probe' tests were adapted from monorepo fresh-module spec-loads to a package-native cache reset (a package module can't be spec-loaded standalone); the store-only byte-identity-vs-playground calibration test was dropped from the library (apps/oakenscrolls-office doesn't exist in the extracted repo). The last two measuring-panel instruments wired 2026-08-11, completing all five ASPIRATIONAL_CLASSES. EXECUTION (stores/instrument_execution.py, opt-in --with-execution): kartikeya per-file parse -- each source file run through its language PARSER (ast.parse/php -l/node --check/bash -n) INSIDE bite 0's sandbox, flagging files that don't parse ('run it, don't read it', the box's load-bearing discipline). Ships file CONTENT base64'd into a sandbox temp (the build dir is NOT mounted -- strictly safer than mounting; the build's code is never run in place, only its text parsed). SAFE BY DEFAULT: require_isolation=True raises InstrumentUnavailable rather than parse untrusted code with no real sandbox, so a bwrap-less host honestly names execution uncovered. bwrap installed this session; verified live -- flags a syntax-broken file, passes a clean one, converges per-file with census/hygiene on a bad file. CALIBRATION (stores/calibration.py + stores/calibration_ledger.py): the confidence mirror, NOT a per-build instrument (calibration is a claim about the model ACROSS builds) -- a longitudinal ledger. calibration.py vendors oakenscrolls-office's math BYTE-FOR-BYTE (brier/log_score/bins/summary; Apache-2.0, guarded by a byte-identity test against apps/oakenscrolls-office/calibration.py). The ledger: a prediction is a (confidence, outcome) pair -- record_prediction (confidence in [0.5,1.0] per D3, rejected not clamped outside; idempotent on the claim via a deterministic hash id), resolve_prediction (refuses double-resolve and unknown ids -- the single-use lesson from the governance resume bug), scorecard (summary+bins over resolved pairs, with a pending count so a thin card reads thin), overconfidence_signal (routes ONE deduped 'review' nudge via checkpoint_governance.route_nudge when overconfidence>=0.10 with >=5 resolved -- one standing open flag per builder; NEVER blocks). Per-builder via soil_store (D6). Verified live: 5 predictions stated 0.9 that hit 0.4 grade overconfidence +0.5 and route one human_required review item. MODULE-IDENTITY BUG fixed en route: an instrument spec-loading its OWN second copy of measure_panel gets a DISTINCT InstrumentUnavailable class, which run_panel's `except InstrumentUnavailable` misses -- mislabeling a real coverage gap as 'errored' not 'COULD NOT RUN' (found live in the 4-instrument panel). Instruments now reuse the one already-loaded measure_panel (guard in instrument_execution + instrument_callgraph), and the CLI registers its __main__ module under 'measure_panel' via sys.modules.setdefault -- so 'could not run' vs 'errored' stays honestly distinct (the sigmap health-vs-coverage lesson applied to the panel's own plumbing). Also hardened two bite-0 forge_build tests that silently assumed no-bwrap (now skip/adapt when bwrap is present). Store-side (D1); apps/the-forge untouched. An adversarial audit confirmed the module-identity fix and the calibration ledger sound (double-resolve/re-record/unknown-id all refused; NaN/inf confidence fails closed; dedup + root routing correct), and found TWO real bugs in the execution instrument, both now fixed + regression-tested: (1) HIGH -- _parser_missing scanned the parser's own stderr for 'not found' to detect a missing binary, but a real SyntaxError echoes the offending SOURCE line, so any parse failure whose broken line contained 'not found' was silently SKIPPED not flagged (a false negative in the instrument's core job); now keys ONLY on exit 127 (the universal command-not-found code; no parse-checker returns 127). (2) MEDIUM -- isolation was checked AFTER the first file's parser already ran, so on a bwrap-less host the first file was parsed in PLAIN mode before the fail-closed raise; now isolation is probed once with a trivial no-op BEFORE any file content is dispatched. (3) LOW -- the Python parser read with locale encoding (valid UTF-8 misread as a parse fail under a C-locale sandbox); now reads bytes so ast.parse honors the coding cookie. All three verified live against real kartikeya. New tests: calibration 5 + calibration_ledger 11 + instrument_execution 10 (+2 regression) = 26; full forge suite 189 passed, 6 skipped. Branch restarted from master after PR #178 merged. First REAL fleet instrument wired into the measuring panel 2026-08-11: stores/instrument_callgraph.py drives codebase-memory-mcp (the call-graph tool that caught the box's decoy) one-shot via cli --json, computing dead code as the SET DIFFERENCE all_functions - called - entry_points - builtins (its OPTIONAL-count aggregate is broken, returns 1 for an unmatched match, so fan_in can't come from one query), emitting per-file fan_in=0 findings. Opt-in via panel CLI --with-callgraph (needs the external binary; DEFAULT stays pure). Degrades to InstrumentUnavailable when the binary is absent, so the panel names call-graph covered only when it truly ran. Verified end-to-end: flags a check_login-shaped dead function nothing calls (the box's decoy), and a fully-wired program shows zero dead code. Pure dead-code core unit-tested against captured output; real drive skipif-guarded. 157 tests green (+6 instrument_callgraph). The measuring panel (model side layer 1) LANDED 2026-08-11 (docs/design/the-forge-measure.md), prompted by the box (rudi193-cmd/quick-stupids PR #5). The checkpoint loop governs the WATCHED decision; the box's real disease was one nobody decided (a committed error_log), caught only because four blind instruments CONVERGED on it. stores/measure_panel.py runs measuring instruments across a build, treats convergence (>=2 instruments naming one artifact) as the alarm, routes convergent findings into the human_required queue (reusing route_nudge, deduped), and reports its OWN coverage honestly (the sigmap health-vs-coverage lesson: names the classes it did NOT measure with the fleet tool for each, so green != sound). Ships framework + 2 dependency-free instruments (census: one-file-dominates; hygiene: committed-by-accident smells); codebase-memory-mcp (call-graph, confirmed pip-installable), kartikeya (execution), oakenscrolls-office (calibration) are wired-next adapters, named as uncovered until then (rule 11). Needs no live model. Store-side (D1); apps/the-forge untouched. 149 tests green (+9 measure_panel). D7-A declared-not-ambient model routing LANDED 2026-08-11: stores/model_route.py is the routing GATE (model inference itself stays stubbed). route(manifest) -> local (loopback vLLM, no net) vs cloud, refusing cloud unless the manifest declares a cloud_llm_fallback permission -- only then does the run get Kart allow_net. Detection reuses willow-mcp's fail-closed is_local_host (vendored into stores/model_egress.py, 3 functions byte-identical to upstream); authorization is NOT a separate signed envelope -- the permission rides inside the D4-signed manifest so the sap-gate signature binds it to the maker (egress_authorization per-task signing noted as later hardening). Store-side (D1). Verified live (local/cloud/refused) + fail-closed on unresolvable/garbage hosts. 136 tests green (+8 model_route). human_loop adopted under checkpoint (D7-B foundation) LANDED 2026-08-11 (docs/design/the-forge-human-loop.md): the D12 move at the governance layer. Nestor seal = memory; willow-mcp's vendored human_loop (stores/human_loop.py, byte-for-byte, Apache-2.0) adds the non-forgeable attestation + the human_required queue over a FilesystemSoilStore (stores/soil_store.py; Nestor's SqliteStore has no generic KV). 'Authorship is not authority' -- run_checkpoint now attests on every memory-backed commit (attestation_id on CheckpointOutcome, by_human, keyed on Nestor pair_id; anti-forgery structural via D1). Plus the async pause seam (park_checkpoint enqueues the model's proposal as evidence when no human is present; resume_checkpoint lets a human seal+attest later -- single-use, a self-audit caught a double-resume bug and fixed it via the queue item's own status) and the nudge outbox (route_nudge persists bite 3/#67 signals as review/overload queue items, deduped by source_ref, closing #67's durability gap). stores/checkpoint_governance.py is the wrapper. Store-side (D1); apps/the-forge untouched. 124 tests green (checkpoint 20 + memory 33 + calibration 14 + schedule 16 + engagement 9 + nudge 15 + soil 9 + governance 8). This is the substrate D7's real model routing plugs into. #67 mid-session nudge LANDED 2026-08-11: stores/checkpoint_nudge.py, two monitors, both pure signals that never block. SessionMirrorMonitor wires the UNUSED half of friction_floor.py (FrictionFloor.scan, the mirror detector) over the maker<->Forge transcript with cross-scan dedup by tripping-turn (the 'injection timing' the reuse-map said was missing); watches real dialogue once D7 lands (stubbed now). EngagementRunMonitor mirrors FrictionFloor's window/episode/re-arm over the per-checkpoint engagement stream (a run of rubber-stamps), reusing checkpoint_engagement.RUBBER_STAMP_FLOOR; None readings (auto/recognize confirm, deferral) are skipped. Verified live: mirroring session flags once at turn 7; a run of thin decisions nudges once per sustained episode, and again only after the trailing-window mean clears the floor (a single moderate decision mid-run does not re-arm -- deliberate, nudge-not-nag). Out-of-[0,1] engagement readings are clamped so a spurious high value can't silently blind the run detector. An adversarial audit found no correctness bug; its two findings (an untested second-distinct-episode dedup property, and a re-arm the commit narrative overstated) are both now fixed/tested. Every willow-mcp piece the reuse-map named for the loop is now wired. 102 tests green (checkpoint 15 + memory 33 + calibration 14 + schedule 16 + engagement 9 + nudge 15); pure/model-free (nudge tests run without the venv). Engagement->grade wire CLOSED 2026-08-11: a resurface-held review now asks 'it still holds - why?' (Responder.justify, optional/duck-typed), scores the rationale via checkpoint_engagement, and feeds checkpoint_schedule.grade(held, engagement) -- a re-argued hold grades FSRS Easy (verified live: next review ~10 days out), a declined one Good (~10 min), a thin one Hard (~5 min). Non-punitive: the hold is never blocked; a Responder without justify reverts to pre-wire Good. This is the piece that makes bite 3's signal actually move the schedule. Also fixed the bite-3 audit finding: RUBBER_STAMP_FLOOR is now the single owned constant checkpoint_schedule.grade() imports for its Hard cutoff (was two coincidental 0.34 literals), guarded by a real cross-module boundary test. 87 tests green (checkpoint 15 + memory 33 + calibration 14 + schedule 16 + engagement 9). Bite 3 (the engagement gate, D8/#66) landed 2026-08-11: stores/friction_floor.py vendored byte-for-byte from willow-mcp (Apache-2.0, second vendor hop willow-gate->willow-mcp->here; code body verified identical to upstream), stores/checkpoint_engagement.py points its sycophancy scorer at the maker's own rationale. run_checkpoint now scores every fresh socratic rationale and surfaces engagement (0-1) + rubber_stamp on CheckpointOutcome -- a pure signal that NEVER blocks a seal (verified: a thin rationale flags rubber_stamp but still seals). RUBBER_STAMP_FLOOR=0.34 is the same line grade() uses for Hard, so the signal and the FSRS grade band cannot disagree. Rule 11 recorded in the vendor header. Open next increments: #67 mid-session nudge, and moving the FSRS grade off engagement on a resurface-held review. 83 tests green (checkpoint 15 + memory 33 + calibration 10 + schedule 16 + engagement 9). FSRS fold-in landed 2026-08-11: stores/checkpoint_schedule.py finishes bite 2's deferred 'is it due' half with real py-fsrs (PyPI fsrs, MIT, soft dependency per D-FSRS-4 -> fixed-interval fallback when absent). resurface now records an FSRS review after every held (->Good) / regressed (->Again) outcome, keyed on the decision's stable Nestor pair_id (verified stable across a held->regressed->held cycle), and reports next_due on ResurfaceOutcome. Design forks settled in docs/design/the-forge-fsrs.md; card state lives in a Forge-owned sidecar (<builder>.schedule.json, 0600), never inside the signed Nestor seal. Bite 2's fixed-interval is_due placeholder retired. 69 tests green (checkpoint 10 + memory 33 + calibration 10 + schedule 16). Bite 2 (the calibration engine, D9/#12+#3) wired 2026-08-11: stores/checkpoint_calibration.py resurfaces a sealed decision and asks whether the maker still holds it. Held -> light confirm, no re-seal; regressed (the practical #3 contradiction signal) -> reject_match(target_text=prior) + fresh Socratic + reseal, verified live against real Nestor to be in-place and non-blocking (a second resurface reflects the NEW answer). Reuse discipline (rule 11): no new matcher (contradiction is Nestor's own ConflictingSealError, reused unwrapped as CheckpointConflict via contradictions()); no new scheduler (is_due is a stdlib placeholder). Scheduler reuse pick settled by the Apache-compat reuse-map (docs/design/the-forge-reuse-map.md): py-fsrs (PyPI fsrs, MIT) -- NOT a willow-mcp 'engram', which the reuse-map proved does not exist (phantom self-citation corrected in docs/design/the-forge.md, rule-11 corollary). 54 tests green (checkpoint 10 + memory 33 + calibration 11). Bite 1 (the checkpoint interaction, D8) wired 2026-08-11: stores/checkpoint.py is the three-band router (auto/recognize/socratic) on top of the already-built D12 memory (stores/checkpoint_memory.py, now soft-Nestor per oakenscrolls-office PR #3). Loose recognition verified live: a reworded-same decision routes to the recognize band and reseals without a fresh Socratic pass (socratic->recognize->auto); 'you choose' seals as a taught deferral; memory degrades to full-Socratic when Nestor is absent. Bite ladder + willow-mcp reuse map in docs/design/the-forge.md. Bite 0 (the spine) wired earlier same day: stores/forge_build.py runs one build end-to-end through the real trust boundary - run_scoped_build (Kart, scoped) -> Plan -> sap_gate.sign_manifest (real D4) -> seam.cross (applies); crown-jewel verified (a '../../escape.py' plan is denied, nothing escapes). the_forge core stays import-pure (D13). Prior state: design-phase scaffold per docs/design/the-forge.md (D1-D13)."
}
