{
  "app_id": "utety-chat",
  "majors": [
    "python",
    "rust",
    "browser"
  ],
  "relation": "unrelated-bundled",
  "anchor": null,
  "location": "apps/utety-chat",
  "maker": "USER",
  "lane": null,
  "state": "gated",
  "notes": "Filed under python (23 py files vs. 5 rs, ~13 in web/). Caveat: 'unrelated-bundled' is the closest closed-vocabulary term, not an accurate one for the whole record. python+rust (campus/ — a full ratatui crate that spawns campus_consult.py as a subprocess) is a verified sidecar relation per docs/store_refit_survey.md; the web/ Cloudflare Pages slice is a third axis that isn't a competing implementation of anything, just a legitimate alternate deploy surface for the same chat product. No term in VALID_RELATIONS names that shape — recorded here per the operator's explicit choice to accept this mischaracterization for the browser slice rather than grow the vocabulary or add a special-case rule for two builds. In CI via the app-tests matrix (store-ci.yml), hence gated. See docs/store_refit_survey.md's 'Open gates' section and the now-resolved entry this replaces in stores/pending.json."
}
