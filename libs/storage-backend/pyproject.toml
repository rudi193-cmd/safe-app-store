[project]
name = "storage-backend"
version = "0.1.0"
description = "Protocol-based SOIL storage seam: put/list/delete with injectable backend."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
