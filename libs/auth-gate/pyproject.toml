[project]
name = "auth-gate"
version = "0.1.0"
description = "AuthGate Protocol + shared build_signed_header for WillowGate-compatible auth."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
