{
  "$pointer": ".willow/store/catalog.json",
  "_note": "catalog.json moved here per docs/store_refit_plan.md P3 (rule 9: the catalog lives in .willow/store/). This root file is a pointer only, kept so anything that still looks at the repo root finds a clear redirect instead of a 404 or a stale copy. Read .willow/store/catalog.json for the real content."
}
