{
  "name": "band-camp-arcade",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Six small, silly, entirely local browser toys for marching band kids. No build step, no backend.",
  "scripts": {
    "serve": "node test/serve.js",
    "test": "node test/run.js",
    "test:mutations": "node test/run.js --mutations"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
