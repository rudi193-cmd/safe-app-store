[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "safe-app-ask-jeles"
version = "0.1.0"
description = "Jeles, your AI librarian. Search the verified web — Smithsonian, Library of Congress, NASA, NIH, and more."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
]
