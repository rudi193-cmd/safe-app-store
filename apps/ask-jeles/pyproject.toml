[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "safe-app-ask-jeles"
version = "0.1.1"
description = "Jeles, your local-first AI librarian for KB, open web, maps, Special Collections, topic trivia, and consented learning events."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "textual>=0.47.0",
    "fastapi>=0.100.0",
    "uvicorn>=0.23.0",
    "pyspellchecker>=0.8.1",
    "rapidfuzz>=3.0.0",
    "mcp>=1.6.0,<2.0.0",
    "jeles @ git+https://github.com/rudi193-cmd/jeles@claude/new-ledger-piece-e3vxvn",
]

[project.scripts]
ask-jeles = "askjeles.crown:main"
askjeles-corpus-mcp = "askjeles.corpus_server:main"
askjeles-seed-easter-egg = "askjeles.seed_easter_egg:main"

[tool.setuptools]
py-modules = ["safe_integration"]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.setuptools.packages.find]
include = ["askjeles*"]
