{
  "app_id": "homestead-health",
  "name": "homestead-health",
  "version": "0.0.1",
  "safe_version": ">=2.0.0",
  "b17": "HHLTH",
  "agent_type": "tool",
  "description": "Family health records the household holds itself — module three on Homestead · Affairs, pinning homestead.keep. Bite 1 (the seat) only: the engine pin proven, invariants H-1..H-5 pending as strict xfails. No network, no listener, synthetic data only until the export/log story (bite 5) is built and audited.",
  "author": "USER",
  "data_streams": [
    {
      "id": "health_records",
      "purpose": "Household health records (roster, immunizations) stored locally through homestead.keep.record",
      "retention": "permanent local; leaves only via purposed, ledgered export (S4)",
      "description": "Not yet written — bite 1 stores nothing. Declared ahead so the stream exists before the first byte does; bites 2-3 write it, under the rungs declared in the design doc."
    }
  ],
  "permissions": [
    "file_write"
  ],
  "privacy_tier": "client_only",
  "local_processing": 1.0,
  "license": "Apache-2.0",
  "repository": "https://github.com/rudi193-cmd/safe-app-store"
}
