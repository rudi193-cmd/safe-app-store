{
  "_staged": "This is the PREPARED attestation. It is under docs/promotion/ (not the app root) on purpose: promote_check reads promotion.json at the candidate ROOT, so keeping it here means the store does not carry a live 'promoted: true' claim before the extraction is real. At extraction, move this file to the extracted repo's root. The two [A] gates (own_repo, host_repointed) become honest only then — see docs/promotion/README.md.",
  "app_id": "homestead-health",
  "author": "vishwakarma",
  "verified_by": "sean",
  "repo_url": "https://github.com/rudi193-cmd/homestead-health",
  "host": "safe-app-store",
  "core_module": "homestead_health",
  "semantic_seam": "homestead_health.reference_lane:Reader",
  "host_repointed": true,
  "major": "python"
}
