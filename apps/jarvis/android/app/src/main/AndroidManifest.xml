<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Required for reminders to fire at the time they were set.

        POST_NOTIFICATIONS, WAKE_LOCK and RECEIVE_BOOT_COMPLETED are merged in
        automatically from the local-notifications plugin's own manifest.
        SCHEDULE_EXACT_ALARM is not — without it, Android 12 and later silently
        downgrade every scheduled notification to inexact, so a reminder set for
        09:00 arrives whenever the system feels like batching it. Nothing fails
        and nothing warns; the reminders just drift.

        Even with the permission declared the user can revoke exact alarms in
        system settings, which is why the bridge probes
        checkExactNotificationSetting() at startup and records the answer rather
        than assuming this line is sufficient.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
</manifest>
