# nest-seed has no hard dependencies: plain text, code, JSON, and --llm
# (stdlib urllib → local Ollama) all work with zero third-party packages.
#
# The packages below unlock extra source file types. They are optional —
# nest-seed degrades gracefully if any are missing — but `make install`
# pulls them so the app runs at full capability out of the box.
#
# System packages (install separately): tesseract-ocr, poppler-utils.
pytesseract>=0.3.10
Pillow>=10.0.0
pdfplumber>=0.10.0
pdf2image>=1.16.0
python-docx>=1.2.0
